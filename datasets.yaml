version: 1
pillar: research-space-systems
title: "Space systems & orbits"
streaming_preferred: true

huggingface:
  - repo_id: nyu-mml/glue
    config: cola
    split: train[:2%]
    streaming: true
    note: Tiny GLUE stream for CI / dataloader sanity — replace with domain datasets as charters land.
  - repo_id: Salesforce/wikitext
    config: wikitext-2-raw-v1
    split: train[:1%]
    streaming: true
    note: >-
      Pinned for `CHARTER_WIKITEXT_STREAM_SMOKE` CI + pytest stream smoke — long-text
      streaming/dataloader sanity only (not astrodynamics, orbits, or trajectory evidence).

kaggle: []


search_keywords: []

reference_streams:
  - name: NASA data portal (reference)
    url: https://data.nasa.gov/
    note: Mission-specific feeds; verify dataset-specific license before mirroring.

  # Next domain-native Hugging Face pins should target NASA- or space-physics-relevant HF
  # datasets once license + streaming/read path are verified, or trajectory/ephemeris
  # tables published in an explicit tabular/API form — do not add placeholder `repo_id`
  # values ahead of confirmation.
